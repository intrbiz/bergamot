The alert for cluster #{slack_encode(cluster.summary)} has been acknowledged by #{slack_encode(notification.acknowledgedBy.summary)} #{dateformat('HH:mm:ss', notification.raised)} on #{dateformat('EEEE dd/MM/yyyy', notification.raised)}
<# slack_message.attachment().color('#55aaff').authorName(notification.acknowledgeSummary).text(notification.acknowledgeComment);
   slack_message.attachment().color('#2c2c2c').text().link('https://' + site.name + '/cluster/id/' + cluster.id, 'View cluster ' + cluster.summary);
#>
