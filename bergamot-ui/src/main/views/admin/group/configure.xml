<!DOCTYPE balsa SYSTEM "http://balsa.intrbiz.net/balsa.dtd">
<?RenderLibrary com.intrbiz.balsa?>
<fragment xmlns="com.intrbiz.balsa" title="Configuration for group #{coalesce(group.summary, group.name)}">
    <div class="row">
   		<h4>XML Configuration</h4>
   		<pre>#{group.toString()}</pre>
    </div>
    <div class="row">
   		<h4>Resolved Configuration</h4>
   		<p class="text-body">The following is the resultant configuration once templates have been applied:</p>
   		<pre>#{group.resolve().toString()}</pre>
    </div>
    <div class="row">
    	<a href="/admin/changeset/add/group/id/#{group.id}" class="add">Edit this group</a>
    	<a href="/admin/changeset/remove/group/id/#{group.id}" class="remove">Remove this group</a>
    </div>
</fragment>